<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.bbs.service.BoardMapper">
	<insert id="insert" parameterType="com.example.demo.bbs.service.Board">
		insert into boards(content, writer_id, reg_date) 
        values (#{content}, #{writerId}, now())
	</insert>
	<select id="select" resultType="com.example.demo.bbs.service.Board">
		select bd_num bdNum,
			   content,
			   writer_id writerId,
			   reg_date regDate
		from boards
	</select>
	<select  id="selectById" resultType="com.example.demo.bbs.service.Board" parameterType="string">
		select bd_num bdNum,
			   content,
			   writer_id writerId,
			   reg_date regDate
		from boards
		where bd_num like #{bdNum}	   
	</select>
</mapper>